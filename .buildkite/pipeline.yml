agents:
  image: "docker.elastic.co/ci-agent-images/ems/buildkite-agent:latest"
  cpu: "2"
  memory: "1G"

steps:
  - label: ":hammer_and_wrench: Build"
    commands: 
      - "yarn install"
      - "yarn build"
